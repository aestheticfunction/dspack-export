<script setup>
/**
 * Intentionally under-typed component: props declared as a string array carry
 * no type information, so extraction keeps them as "unknown" and warns.
 */
defineProps(['alpha', 'beta'])
</script>

<template>
  <div />
</template>
